name: rl-training
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip>=23.0
  - pip:
    - --no-cache-dir
    - tianshou==2.0.0
    - gymnasium>=0.29.0
    - torch>=2.0.0
    - numpy>=1.21.0
    - pygame>=2.0.0
    - matplotlib>=3.5.0
    - ipython>=8.0.0
    - jupyter>=1.0.0
    - pyyaml>=6.0
